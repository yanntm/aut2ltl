# Named-case campaigns — the committed record

Generated by `python3 -m tests.sosl.campaign_manifest` (from `sosl/`) — a build
product, not a hand-edited file. Every figure below is recomputed from the
`results.csv` in each directory.

- date: 2026-07-11
- git: 3ef66c3ca

These campaigns run hand-chosen specimens, not the catalogue (that is
`reference/census/`). Each is a **gate**: it asserts its claim and exits nonzero if
it breaks, so re-running it is both the reproduction and the check. Unlike the
census, they do not depend on the corpus, so a re-run *is* a reproduction.

## `e0/`

The E0 gate: zero failures, zero budget overruns, zero crashes on the named specimens. The permanent specimens certify `ACCEPTOR_ONLY` under no-saturation (spec §9 P4/F5), and the Even/EvenBlocks ledgers are byte-stable (row P5).

- 10 runs = 6 case(s) x 3 config(s)
- verdicts: **ACCEPTOR_ONLY** 2, **SOUND** 8
- cases: `a_implies_xa`, `a_once`, `even`, `evenblocks`, `gf_aa_parity`, `gf_aa_reset`
- configs: `default`, `exact`, `no-sat-exact`
- artifacts: `results.csv, e0_report.md, e4_transcripts.md`

Regenerate (from `sosl/`) — the campaign is its own gate, so a nonzero exit *is* the
failure report:

```
python3 -m tests.sosl.campaign_e0
```

## `m4b/`

The E1 cost bound holds on the named cases, and the E2 stall classes match theory (the permanent specimens are `a_implies_xa`, `a_once`).

- 12 runs = 6 case(s) x 2 config(s)
- verdicts: **ACCEPTOR_ONLY** 2, **SOUND** 10
- cases: `a_implies_xa`, `a_once`, `even`, `evenblocks`, `gf_aa_parity`, `gf_aa_reset`
- configs: `default`, `no-sat-exact`
- artifacts: `results.csv, e1_report.md, e2_report.md`

Regenerate (from `sosl/`) — the campaign is its own gate, so a nonzero exit *is* the
failure report:

```
python3 -m tests.sosl.campaign_m4b
```

## `e5/`

Counterexample padding preserves the invariant — only cost changes: every policy learns the same object, so `cex-first` is as sound as `cex-minimal`.

- 28 runs = 4 case(s) x 7 config(s)
- verdicts: **SOUND** 28
- cases: `a_once`, `even`, `evenblocks`, `gf_aa_parity`
- configs: `cex-first`, `cex-minimal`, `cex-padded:16`, `cex-padded:2`, `cex-padded:32`, `cex-padded:4`, `cex-padded:8`
- artifacts: `results.csv, e5_report.md`

Regenerate (from `sosl/`) — the campaign is its own gate, so a nonzero exit *is* the
failure report:

```
python3 -m tests.sosl.campaign_e5
```
