"""
aut2ltl.bls.definability — syntactic-ω-semigroup constructions over a `Language`.

Three subpackages, each imported directly:

  * `oracle` — the exact LTL-definability decision (LTL / NOT_LTL with a replayed
    counting family / INCONCLUSIVE);
  * `dg` — synthesis of a defining LTL formula from the aperiodic quotient;
  * `witness` — the seeded counting-family extractor.
"""
