% sos2cayley evenblocks.sos --rename a=a,!a=b
% Generated by sosl.sos.viz. Every visual choice is in the \tikzset below:
% edit a style to restyle, edit a coordinate to move a node.
\documentclass[tikz,border=4pt]{standalone}
\usepackage{amssymb}
\usetikzlibrary{arrows.meta,shapes.misc}
\begin{document}
\begin{tikzpicture}[
  class/.style     = {draw, thin, rounded corners=2pt, inner sep=3pt,
                      minimum width=10mm, minimum height=7mm},
  root/.style      = {},          % the root needs no ink: the init stub says it
  init/.style      = {semithick, -{Stealth[length=4pt,width=3pt]}},
  arrow/.style     = {thin, -{Stealth[length=4pt,width=3pt]}},
  lbl/.style       = {font=\small, inner sep=1.5pt, fill=white},
  pairs/.style     = {anchor=north, font=\small},
]

  \node[class,root] (eps) at (2.2,2.6) {$[\varepsilon]$};
  \node[class] (a) at (0.0,0.9) {$[a]$};
  \node[class] (b) at (4.4,0.9) {$[b]$};
  \node[class] (aa) at (2.2,0.9) {$[a{\cdot}a]$};
  \node[class] (ab) at (0.0,-1.7) {$[a{\cdot}b]$};
  \node[class] (ba) at (4.4,-1.7) {$[b{\cdot}a]$};
  \node[class] (aba) at (0.0,-4.3) {$[a{\cdot}b{\cdot}a]$};
  \node[class] (bab) at (4.4,-4.3) {$[b{\cdot}a{\cdot}b]$};

  % the root is the adjoined identity: a source, marked like an initial state
  \draw[init] (2.2,3.5) -- (eps);

  \draw[arrow] (eps) to node[lbl] {$[a]$} (a);
  \draw[arrow] (eps) to node[lbl] {$[b]$} (b);
  \draw[arrow] (a) to[bend left=15] node[lbl] {$[a]$} (aa);
  \draw[arrow] (a) to node[lbl] {$[b]$} (ab);
  \draw[arrow] (b) to[bend left=15] node[lbl] {$[a]$} (ba);
  \draw[arrow] (b) to[loop right] node[lbl] {$[b]$} (b);
  \draw[arrow] (aa) to[bend left=15] node[lbl] {$[a]$} (a);
  \draw[arrow] (aa) to node[lbl] {$[b]$} (b);
  \draw[arrow] (ab) to[bend left=15] node[lbl] {$[a]$} (aba);
  \draw[arrow] (ab) to[loop left] node[lbl] {$[b]$} (ab);
  \draw[arrow] (ba) to[bend left=15] node[lbl] {$[a]$} (b);
  \draw[arrow] (ba) to node[lbl] {$[b]$} (bab);
  \draw[arrow] (aba) to[bend left=15] node[lbl] {$[a]$} (ab);
  \draw[arrow] (aba) to node[lbl] {$[b]$} (bab);
  \draw[arrow] (bab) to[loop right] node[lbl] {$[a],[b]$} (bab);

  \node[pairs] at (2.2,-5.6) {$\lambda:\; a \rightarrowtail [a],\;\; b \rightarrowtail [b]$};
  \node[pairs] at (2.2,-6.3) {$P = \{\, ([b],[b]),\; ([a{\cdot}b],[b]),\; ([b{\cdot}a],[a{\cdot}b{\cdot}a]),\; ([a{\cdot}b{\cdot}a],[a{\cdot}b{\cdot}a]),\; ([b{\cdot}a{\cdot}b],[b]),\; ([b{\cdot}a{\cdot}b],[a{\cdot}b{\cdot}a]) \,\}$};
\end{tikzpicture}
\end{document}
