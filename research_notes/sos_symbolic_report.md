# SoS Symbolic Engine — results (active ledger)

The engineering response to `research_notes/sos_symbolic_spec.md`:
one finding row per checked or refuted prediction, or per mechanism a
run settled. **Findings F1–F26 are closed and retired** to
`sos_symbolic_experiments.md` (the frozen archive — M1 through C10
§6.2, E0 and E1, plus the Theory responses); the paper's measured
claims (§8, RQ1–RQ5) trace there. This file receives **new findings
only, from F27**, and is periodically retired to the archive when a
campaign closes.

Engine: `sos_sdd/` (libDDD multi-valued DDD core behind a Python
`SoS` API; backend decisions recorded in `sos_sdd/README.md`). Data
conventions: tracked CSVs under `tests/sos_sdd/reference/`, streams
under `tests/sos_sdd/logs/`, budgets are findings.

## Open ledger

*(empty — awaiting the next campaign: C10 remainder + E9, C9
remainder + E7/E8, E2's second family, E3, E4, E5, E6. See the
spec's work items and `handoff_symbolic.md` for the queue.)*
