# A Calculus on the Syntactic ω-Semigroup: Align, Operate, Reduce

**Yann Thierry-Mieg**

With significant inputs from
**Claude (Anthropic)**

*Skeleton draft — 2026-07-07 — placeholders marked `⟨TBD: …⟩`*

## Abstract

The syntactic ω-semigroup of an ω-regular language is now constructible
[SωS26], learnable [SωSL26], and exploitable for definability [SωSX26].
This paper proposes it as something more mundane and more consequential: a
*computational substrate* — the object on which the everyday operations of
an ω-automata toolbox (Spot's, say) are performed, instead of on automata.
The calculus has three primitive moves: **align** two invariants on a
common table (a generated product, the only polynomial-priced move),
**operate** by surgery on the pair set `P` (where almost every operation
lives, almost always for free), and **reduce** to the canonical object
(re-quotient, polynomial). Complement — `2^{Θ(n log n)}` on
nondeterministic Büchi automata — is one bit-flip. Equivalence — PSPACE on
automata — is byte equality. Left quotients, rootings, pair languages,
inverse substitutions: free surgeries. Classification checks that Spot
implements as constructions (stutter-invariance, safety/co-safety,
obligation, the acceptance strength actually needed) are equations on the
table, read off. The exponentials do not disappear — they concentrate,
exactly at the ω-rational constructors (concatenation by a prefix set,
ω-power) and existential projection, where a powerset is intrinsic. The
resulting picture is a *pay-canonicity-once* economy: entering the
calculus costs what determinization always cost; staying in it makes
everything downstream cheap, normal-formed, and certificate-producing.
⟨TBD: headline complexity table; experimental comparison against Spot on
the census corpus.⟩

---

## 1. Introduction

⟨TBD: full pass. The arc: automata toolboxes pay per-operation —
products for Boolean operations, Safra for complement, PSPACE for
equivalence, bespoke constructions for every classification check — and
produce presentation-dependent results that must be re-simplified after
every step. The syntactic ω-semigroup `𝓘(L) = (𝒞, λ, M, P)` is complete
and canonical [SωS26, Thm 5.1]; this paper reads that theorem as an API.
Thesis: one alignment move, then a calculus of pair-set surgeries with a
normal form. Contributions: (1) the three-move decomposition and the
free-surgery catalog (§3); (2) the ledger against a production toolbox,
operation by operation, with the exponential frontier located exactly
(§4); (3) read-off replacements for classification constructions,
including a one-scan stutter-invariance test (§3.5); (4) ⟨TBD:
implementation + measurements⟩.⟩

## 2. Background: the object and its oracle

⟨TBD: compressed recall of [SωS26]: the invariant `(𝒞, λ, M, P)`; the
membership oracle
`Val(c, d) = [ (c·d^π, d^π) ∈ P ]`; linked pairs; byte-equality
completeness (Thm 5.1); the reduction (re-canonicalization) procedure and
its complexity [SωS26, §8]. Notation: `𝓘₁, 𝓘₂` two invariants over a
common alphabet `Σ`; `n_i = |𝒞_i|`.⟩

## 3. The calculus

### 3.1 Three primitive moves

Every operation below factors through three moves:

1. **Align.** Put two invariants on one table: the *generated product*
   `𝓘₁ ⊗ 𝓘₂`, with class set the submonoid of `𝒞₁ × 𝒞₂` generated by
   `{ (λ₁(a), λ₂(a)) : a ∈ Σ }` (fresh identity adjoined), letter map
   `a ↦ (λ₁(a), λ₂(a))`, componentwise multiplication, and *both* pair
   sets carried along as verdict maps
   `Val_i((c₁,c₂), (d₁,d₂)) = Val_i(c_i, d_i)`. Size at most `n₁·n₂`,
   often far less — only the generated part exists. One alignment serves
   all subsequent operations on the pair.
2. **Operate.** Surgery on pair sets over the fixed table. This is where
   the operations live, and the catalog of §3.2 is the point: almost all
   of them are `O(|P|)` or one scan.
3. **Reduce.** Re-canonicalize: quotient the table by the congruence its
   own `Val` induces [SωS26, §5]. Polynomial; yields *the* syntactic
   object of the result. Reduction is the calculus's normal form — the
   move automata do not have (minimal deterministic ω-automata do not
   exist; simplification is heuristic and model-bound).

The slogan: **align is the only product-priced move; operate is free;
reduce is the normal form.** An operation is expensive exactly when it
cannot be phrased as surgery on an aligned table — §3.4 locates those.

### 3.2 The free fragment: the surgery catalog

All of the following act on a fixed table `(𝒞, λ, M)`; each returns a
pair set on the same table, to be reduced at will. Proposition 5.11 of
[SωSX26] (decomposition never leaves LTL) is the safety net for the whole
fragment: every result's syntactic algebra divides `M`.

- **Boolean algebra.** `P₁ ∪ P₂`, `P₁ ∩ P₂`, `P^c` (relative to the
  linked pairs), differences, xor: the same-table languages form a
  *finite Boolean algebra*, isomorphic to the algebra of saturated pair
  sets. Complement is one flip — against `2^{Θ(n log n)}` for
  nondeterministic Büchi complementation, this is the calculus's
  headline entry.
- **Rooting (left quotients).** For `c ∈ 𝒞¹` define
  `P_c := { (s, e) linked : (c·s, e) ∈ P }`. Then
  `Val_{P_c}(x, d) = Val_P(c·x, d)`, so `L(P_c) = u⁻¹L` for any
  representative `u` of `c` — prefix subtraction is pair surgery. The
  rootings form a right `M`-action, `P_{c·d} = (P_c)_d`, so quotients
  compose as they must (`(uv)⁻¹L = v⁻¹(u⁻¹L)`), and the number of
  distinct rootings *is* the residual count read-off [SωS26, Prop 4.6]:
  the residual automaton of `L`, internalized. These rootings are
  exactly the memoized class children of the extraction [SωSX26, §5.2],
  and Lemma 5.9 there (reach absorption) is a rooting identity.
- **Pair languages and prolongations.** The pair classes are the
  conjugacy classes, and conjugacy is a law about *cells*, not pairs:

  **Proposition 3.1 (conjugacy and saturation).** For every linked pair
  `(s, e)` and every factorization `e = x·y`:
  `s·e^ω = (s·x)·(y·x)^ω`, so the cells `(s, e)` and `(s·x, y·x)` carry
  one verdict. The conjugate cell renormalizes to the linked pair
  `((s·x)·f, f)` with `f = (y·x)^π` — and the renormalization is not
  optional: `x·y` idempotent does not make `y·x` idempotent; only
  `(y·x)²` is guaranteed to be (`(yx)³ = y·(xy)²·x = (yx)²`), so the
  naive pair-level transport `(s, e) ↦ (s·x, y·x)` leaves the linked
  pairs. Two linked pairs denote the same ω-word class iff the closure
  under these renormalized moves connects them [PP04]; a set of linked
  pairs is a language over the table iff it is a union of such classes —
  *saturated*.

  Any saturated `P'` is then a language: a single class gives "the words
  realizing exactly this accepting behavior" — *prolonging the language
  from one of its behaviors*, the finest granularity of the OR-split
  combinator [SωSX26, §5.6], with its Wagner-ladder guard.
- **Inverse substitutions.** For `π : Σ' → Σ` (relabeling, letter
  merging): compose `λ ∘ π`, same table, reduce. Inverse morphic images
  are free; Spot's `relabel` is a special case.
- **Canonical witnesses.** Every nonempty pair set carries its own
  certificate: `(s, e) ∈ P` yields the lasso `key(s)·key(e)^ω`, shortlex
  keys giving *the* canonical witness word. Emptiness is `P = ∅`;
  universality is `P = all linked pairs`; a witness or counterexample is
  read off in the same scan — the certificate discipline of
  [SωSX26, §4], available to every operation. And the witness is not
  merely canonical:

  **Proposition 3.2 (the canonical witness is minimal).** Order lassos
  by stem length, then loop length, then lexicographically, and scan
  cells `(c, d)` in the order of their lassos `key(c)·key(d)^ω`. For any
  property that factors through the membership oracle, the first
  satisfying cell's lasso is the least satisfying lasso *among all
  lassos*: a satisfying `(u, v)` lives in the cell
  `(fold(u), fold(v))`, whose keys are shortlex-least in their classes,
  so the cell's lasso dominates it componentwise. Every certificate the
  calculus emits — an emptiness witness, an inclusion or equivalence
  counterexample — is therefore the minimal one, and a client consuming
  counterexamples (the learner's teacher [SωSL26]) inherits its
  minimal-order guarantee from the scan order alone.
- **Decision procedures as scans.** On reduced objects: equivalence is
  byte equality [SωS26, Thm 5.1]; inclusion `L₁ ⊆ L₂` after alignment is
  the pointwise test `Val₁ ≤ Val₂`, with the first failing pair
  furnishing the canonical separating lasso. Compare: PSPACE-complete on
  automata, with counterexamples needing product-emptiness runs.

### 3.3 The aligned fragment

Cross-table operations pay the alignment price `O(n₁·n₂)` once:

- **Union / intersection / difference across tables**: align, then §3.2.
  Note what is *absent*: no acceptance-condition surgery. Büchi
  intersection needs degeneralization counters, union of deterministic
  models may not exist, generalized conditions need bookkeeping; here
  acceptance is a pair set and conjunction is pointwise `∧` of verdicts.
- **Model-checking-shaped queries**: `L₁ ∩ L₂ = ∅?` (Spot's
  `intersecting_word`) is align + scan of `Val₁ ∧ Val₂` + canonical
  witness. Same product asymptotics as automata, plus the normal form
  and the certificate.
- ⟨TBD: when alignment stays small — shared subalphabets, factored
  specifications; the generated product as the calculus's own notion of
  "on-the-fly".⟩

### 3.4 The exponential frontier

The calculus is honest about where powersets are intrinsic:

- **Concatenation by a prefix set (`W·L`) and ω-power (`W^ω`).** The
  ω-rational constructors quantify existentially over a split position;
  no surgery on an aligned table expresses that — the syntactic object
  of the result can be exponentially larger (already so for syntactic
  monoids of concatenations on finite words). This is where the
  nondeterminism that automata carry natively re-enters; constructions
  exist [PP04] but cost what determinization costs. ⟨TBD: precise
  statements; sharp examples; whether the census (see [SωSN26]) shows
  the blowup is rare on small specimens.⟩
- **Existential projection (`remove_ap`).** Quantifying an atomic
  proposition away is the QPTL wall met in [SωSX26, §6]: a deterministic
  definitional extension is free, a genuine guess is a powerset. Spot
  pays the same, differently distributed.
- **Entry price.** Constructing `𝓘(L)` from a nondeterministic acceptor
  embeds a determinization; the calculus does not evade lower bounds, it
  *relocates* them to the entry gate. The economic claim is
  amortization: **pay canonicity once, then operate in the free
  fragment** — a specification pipeline that complements, conjoins,
  quotients, checks and re-checks pays the exponential once instead of
  at every complement. ⟨TBD: a worked pipeline where Spot pays k times
  and the calculus once.⟩

### 3.5 Read-offs replace constructions

Spot answers classification queries by building automata and testing
them; on the invariant the same queries are equations:

- **Stutter invariance, one scan.**

  **Proposition 3.3.** `L` is stutter-invariant iff `λ(a)·λ(a) = λ(a)`
  for every letter `a ∈ Σ`.

  *Proof sketch.* (⇒) `u·a·v` and `u·a·a·v` are stutter-equivalent in
  every context of both Arnold shapes, so `[a] = [a·a]` in the syntactic
  quotient. (⇐) With every letter class idempotent, any finite word
  folds to the class of its destuttered form; for an ω-word, choose
  Ramsey cut points at letter-change positions (the eventually-constant
  tail is handled directly), so a factorization of `α` maps blockwise to
  one of its destuttered core with the same classes — same pair, same
  verdict. ⟨TBD: full proof; the eventually-constant corner written
  out.⟩ Spot's check builds and compares closure automata; here it is
  `|Σ|` table lookups.

- **The safety–progress ladder** (safety, co-safety, obligation,
  recurrence, persistence, reactivity): closure conditions on `P`
  [SωS26, §7.1] — Spot's `is_safety`, `is_obligation`, … as scans.
- **Acceptance strength needed** (Spot's parity/Rabin-index style
  queries): the acceptance-index read-off [SωS26, §7.3] — a property of
  the language, not of a chosen condition.
- **Wagner degree**: the complete read-off [SωS26, §7.4]; every
  hierarchy query below it specializes.
- **LTL-definability and extraction**: the aperiodicity scan, then
  [SωSX26] for the formula or the counting-family certificate. (Spot has
  no automaton→LTL path.)
- **Hulls, conjecturally.** The safety closure of `L`, its liveness
  part, and the Manna–Pnueli decomposition `L = safety ∩ liveness` look
  like `P`-completions along the ladder characterizations — surgery plus
  reduction, plausibly polynomial. ⟨TBD: work out the safety-hull pair
  set; prove or refute polynomiality; this may deserve its own
  section — the temporal hierarchy as a lattice of hulls on one table.⟩

## 4. The ledger against a production toolbox

⟨TBD: the full table, one row per Spot entry point, columns: Spot
construction & cost / calculus move & cost / output canonicity /
certificate. Draft rows below; verify Spot-side details against the tool
paper — source to be added to the library.⟩

| operation | automata (Spot) | on `𝓘(L)` |
|---|---|---|
| complement | `2^{Θ(n log n)}` (Safra/…) | `P ↦ P^c`, free |
| union / intersection | product (+ acceptance surgery) | align `O(n₁n₂)` + pointwise `∧/∨` |
| emptiness + witness | SCC scan | `P = ∅?`, key-built lasso |
| universality | complement + emptiness | `P = linked?`, scan |
| inclusion / equivalence | PSPACE / simulations | `Val₁ ≤ Val₂` scan / byte equality |
| lasso membership | run the product | one fold through `λ, M`, one `P` lookup |
| left quotient | derivative construction | rooting `P_c`, free |
| relabel / inverse subst. | rebuild | compose `λ`, free |
| determinize | Safra/Zielonka | *meaningless* — object already canonical-deterministic; the cost sits at entry |
| degeneralize / to-parity / acc transforms | bespoke constructions | *dissolved* — acceptance is `P`; the needed strength is a read-off |
| minimize / simulation reductions | heuristic, model-bound | reduce: the normal form, always, uniquely |
| stutter-invariance | closure-automata test | `λ(a)² = λ(a)` scan (Prop 3.3) |
| safety/obligation/… tests | model-specific checks | ladder scans on `P` |
| concatenation `W·L`, `W^ω` | native (nondeterminism) | exponential — intrinsic (§3.4) |
| projection `remove_ap` | subset-flavored | exponential — the QPTL wall (§3.4) |
| automaton → LTL | absent | [SωSX26] on the aperiodic side |

**Exit constructions.** The calculus should end where the consumer
needs: to an NBA of size polynomial in `|𝒞|` via the classical
`⋃ L_s·(L_e)^ω` decomposition over accepting pairs [PP04] ⟨TBD: exact
size; determinized exits⟩; to LTL via [SωSX26] when aperiodic; to the
witness/certificate formats of [SωSX26, §4] always.

**What cannot be simulated.** Anything needing branching semantics
(games, synthesis) — the invariant is a linear-time object. And
succinctness: `𝓘(L)` can be exponentially larger than a good
nondeterministic presentation; the census [SωSN26] measures how often
canonicity actually costs. The honest positioning: the calculus is not a
back-end for one-shot translations; it is the substrate for pipelines
that *keep* a language and work on it.

## 5. Complexity summary

⟨TBD: after the propositions stabilize — one table: move / cost /
output size, with the reduction complexity imported from [SωS26, §8].⟩

## 6. Related work

⟨TBD: skeleton — Spot and the ω-automata toolbox tradition (source to
add to the library); recognizability by ω-semigroups and the ω-rational
operations [PP04]; Wilke algebras [Wil93]; Carton–Michel canonical
automata; the Wagner hierarchy computations [CP97, CP99, SW08];
residual-automata lines. Position: none of these treats the syntactic
object as an *operational* substrate with a normal form.⟩

## 7. Conclusion

⟨TBD: the arc — recognition is a calculus, not a verdict; three moves;
free Boolean algebra with residuals; exponentials concentrated at the
gates where they are intrinsic; one canonical normal form throughout.
The companion census [SωSN26] counts the universe this calculus
operates on; the extraction paper [SωSX26] is its most elaborate derived
operation.⟩

---

## References

- **[CP97]** O. Carton, D. Perrin. *Chains and superchains for
  ω-rational sets…* IJAC, 1997. ⟨TBD: verify exact title against
  library copy.⟩
- **[CP99]** O. Carton, D. Perrin. IJAC, 1999. ⟨TBD: complete entry from
  library copy.⟩
- **[PP04]** D. Perrin, J.-É. Pin. *Infinite Words: Automata,
  Semigroups, Logic and Games.* Elsevier, 2004.
- **[SW08]** V. Selivanov, K. Wagner. Fund. Inform., 2008. ⟨TBD:
  complete entry from library copy.⟩
- **[SωS26]** Y. Thierry-Mieg, with Claude (Anthropic). *Constructing
  the syntactic ω-semigroup from a deterministic Emerson–Lei automaton.*
  Working draft, 2026.
- **[SωSL26]** Y. Thierry-Mieg, with Claude (Anthropic). *Learning the
  syntactic ω-semigroup.* Working draft, 2026.
- **[SωSX26]** Y. Thierry-Mieg, with Claude (Anthropic). *The LTL
  frontier from the syntactic ω-semigroup.* Working draft, 2026.
- **[SωSN26]** Y. Thierry-Mieg, with Claude (Anthropic). *A census of
  syntactic ω-semigroups.* Working draft, 2026.
- **[Wil93]** T. Wilke. *An algebraic theory for regular languages of
  finite and infinite words.* IJAC, 1993. ⟨TBD: verify against library
  copy.⟩
- ⟨TBD: Spot tool paper (Duret-Lutz et al.) — to be added to the
  library; Safra; Michel/stutter-invariance checking lineage.⟩
